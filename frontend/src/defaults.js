export const DEFAULT_DESIGN = `// Type a prompt above or click an example to get started`

export const DEFAULT_TESTBENCH = `// Type a prompt above or click an example in the sidebar to get started`

// SystemVerilog starter — parallel to DEFAULT_DESIGN, mirroring the same
// hint-comment convention. The editor stays in SV mode (lint disabled,
// highlighter still parses it as Verilog) until the user clicks GENERATE.
export const DEFAULT_DESIGN_SV = `// Type a prompt above or click an example to get started`

export const DEFAULT_TESTBENCH_SV = `// Type a prompt above or click an example in the sidebar to get started`
